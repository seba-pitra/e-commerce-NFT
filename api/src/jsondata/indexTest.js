const angryCat = require("../jsondata/angryCat.json").slice(0, 5)
const boki = require("../jsondata/Boki.json").slice(0, 5)
const boredy00tsAC = require("../jsondata/Bored-y00ts-AC.json").slice(0, 5)
const boredApeYacht = require("../jsondata/boredApeYacht.json").slice(0, 5)
const broadside = require("../jsondata/Broadside.json").slice(0, 5)
const c01OfficialCollection = require("../jsondata/C-01-Official-Collection.json").slice(0, 5)
const coolCats = require("../jsondata/coolCats.json").slice(0, 5)
const corntown = require("../jsondata/Corntown-wtf.json").slice(0, 5)
const creepz = require("../jsondata/Creepz-by-OVERLORD.json").slice(0, 5)
const cryptoMories = require("../jsondata/CryptoMories.json").slice(0, 5)
const cryptoPunks = require("../jsondata/cryptoPunks.json").slice(0, 5)
const dEGEN = require("../jsondata/DEGEN-TOONZ.json").slice(0, 5)
const eNS = require("../jsondata/ENS-Maxis.json").slice(0, 5)
const hAPE = require("../jsondata/HAPE-PRIME.json").slice(0, 5)
const kitaroWorld = require("../jsondata/Kitaro-World.json").slice(0, 5)
const lilPudgys = require("../jsondata/Lil-Pudgys.json").slice(0, 5)
const looksRare = require("../jsondata/LooksRare.json").slice(0, 5)
const lOSTPOETS = require("../jsondata/LOSTPOETS.json").slice(0, 5)
const mekaVerse = require("../jsondata/MekaVerse.json").slice(0, 5)
const moonbirds = require("../jsondata/moonbirds.json").slice(0, 5)
const mutantApeYachtClub = require("../jsondata/mutantApeYachtClub.json").slice(0, 5)
const myPetHooligan = require("../jsondata/My-Pet-Hooligan.json").slice(0, 5)
const phantaBear = require("../jsondata/PhantaBear.json").slice(0, 5)
const primeApePlanetPAP = require("../jsondata/Prime-Ape-Planet-PAP.json").slice(0, 5)
const regulars = require("../jsondata/Regulars.json").slice(0, 5)
const rENGA = require("../jsondata/RENGA.json").slice(0, 5)
const supDucks = require("../jsondata/SupDucks.json").slice(0, 5)
const theMV3Universe = require("../jsondata/The-MV3-Universe.json").slice(0, 5)
const tHEPLUTOALLIANCE = require("../jsondata/THE-PLUTO-ALLIANCE.json").slice(0, 5)
const toxicSkullsClub = require("../jsondata/Toxic-Skulls-Club.json").slice(0, 5)
const worldOfWomenGalaxy = require("../jsondata/World-of-Women-Galaxy.json").slice(0, 5)
const YuGiYn = require("../jsondata/YuGiYn.json").slice(0, 5)

const testNFTs = [ 
    ...angryCat,
    ...boki,
    ...boredy00tsAC,
    ...boredApeYacht,
    ...broadside,
    ...c01OfficialCollection,
    ...coolCats,
    ...corntown,
    ...creepz,
    ...cryptoMories,
    ...cryptoPunks,
    ...dEGEN,
    ...eNS,
    ...hAPE,
    ...kitaroWorld,
    ...lilPudgys,
    ...looksRare,
    ...lOSTPOETS,
    ...mekaVerse,
    ...moonbirds,
    ...mutantApeYachtClub,
    ...myPetHooligan,
    ...phantaBear,
    ...primeApePlanetPAP,
    ...regulars,
    ...rENGA,
    ...supDucks,
    ...theMV3Universe,
    ...tHEPLUTOALLIANCE,
    ...toxicSkullsClub,
    ...worldOfWomenGalaxy,
    ...YuGiYn
];

module.exports = testNFTs